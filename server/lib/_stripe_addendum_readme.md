// APPENDED to server/lib/stripe.js (see that file's existing content for
// stripeGet/stripeDelete/verifyStripeSignature/fulfillment functions). This
// is a separate upsert only because of how it was authored — all of this
// belongs in the same file as the rest of stripe.js.
