{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "avatar_url": {
      "type": "string",
      "description": "Optional profile photo URL, set via the user's own profile settings or first-run onboarding. Falls back to initials in the UI when unset."
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}